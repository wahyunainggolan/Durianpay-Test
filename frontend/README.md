How to run frontend on local:
1. Copy and rename env.example from root to frontend directory
```bash
cp env.example frontend/.env
```
2. move to frontend directory and run frontend
```bash
cd frontend
npm run dev
```
3. In case Backend already run before.
